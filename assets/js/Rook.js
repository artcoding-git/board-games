// Generated by CoffeeScript 1.7.1
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  angular.module("app").factory("Rook", [
    "ChessPiece", function(ChessPiece) {
      var Rook;
      return Rook = (function(_super) {
        __extends(Rook, _super);

        Rook.$inject = ["$injector"];

        function Rook($injector) {
          Rook.__super__.constructor.call(this, $injector);
          this.moved = false;
        }

        Rook.prototype.pieceAvailableMoves = function() {
          var cell, result, _ref, _ref1, _ref2, _ref3;
          result = Rook.__super__.pieceAvailableMoves.apply(this, arguments);
          if (this.cell == null) {
            return result;
          }
          cell = this.cell;
          while (((cell = cell.toLeft()) != null) && !cell.piece()) {
            result.push(cell);
          }
          if (((cell != null ? (_ref = cell.piece()) != null ? _ref.player : void 0 : void 0) || this.player) !== this.player) {
            result.push(cell);
          }
          cell = this.cell;
          while (((cell = cell.toRight()) != null) && !cell.piece()) {
            result.push(cell);
          }
          if (((cell != null ? (_ref1 = cell.piece()) != null ? _ref1.player : void 0 : void 0) || this.player) !== this.player) {
            result.push(cell);
          }
          cell = this.cell;
          while (((cell = cell.up()) != null) && !cell.piece()) {
            result.push(cell);
          }
          if (((cell != null ? (_ref2 = cell.piece()) != null ? _ref2.player : void 0 : void 0) || this.player) !== this.player) {
            result.push(cell);
          }
          cell = this.cell;
          while (((cell = cell.down()) != null) && !cell.piece()) {
            result.push(cell);
          }
          if (((cell != null ? (_ref3 = cell.piece()) != null ? _ref3.player : void 0 : void 0) || this.player) !== this.player) {
            result.push(cell);
          }
          return result;
        };

        Rook.prototype.move = function(newCell) {
          this.moved = true;
          return Rook.__super__.move.apply(this, arguments);
        };

        return Rook;

      })(ChessPiece);
    }
  ]);

}).call(this);

//# sourceMappingURL=Rook.map
